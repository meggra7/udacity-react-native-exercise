import { StyleSheet } from "react-native";

export const appStyles = StyleSheet.create({
  scrollViewContainer: {
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
